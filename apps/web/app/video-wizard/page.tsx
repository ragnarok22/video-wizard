import { VideoContainer } from '@/features/video/containers/video-container';

/**
 * Video Wizard Page
 * 
 * Server component that renders the video processing feature
 */
export default function VideoWizardPage() {
  return <VideoContainer />;
}
